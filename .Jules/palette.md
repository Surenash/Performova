## 2024-03-21 - [ARIA Labels for Icon-only Buttons]
**Learning:** Icon-only buttons (like chat widgets) frequently lack `aria-label`s, causing accessibility issues for screen reader users.
**Action:** Always verify `button` elements that only contain SVGs or icons have a descriptive `aria-label`.
